# Only the build needs sources + Gradle metadata; ignore everything else.
*

!gradlew
!settings.gradle.kts
!build.gradle.kts
!gradle.properties
!gradle
!src

# ...but not the recorded Docker fixtures: 276K of test-only corpus in every production build context.
src/test/resources/docker-fixtures
